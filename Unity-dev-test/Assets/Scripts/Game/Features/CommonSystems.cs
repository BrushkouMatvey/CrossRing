using Entitas;

public class CommonSystems : Feature  {
    public CommonSystems(Contexts contexts)
    {
        // Add(new CheckFirstBallPosition(contexts));
        // Add(new MoveSystem(contexts));
        //Add(new PositionSystem(contexts));
        // Add(new DestroySystem(contexts));
    }	
}